/** Max bytes for profile avatar uploads (5 MB). */
export const PROFILE_AVATAR_MAX_FILE_BYTES = 5 * 1024 * 1024;

/** Each side must be at least this many pixels. */
export const PROFILE_AVATAR_MIN_EDGE_PX = 512;

/** Each side must be at most this many pixels. */
export const PROFILE_AVATAR_MAX_EDGE_PX = 4096;
