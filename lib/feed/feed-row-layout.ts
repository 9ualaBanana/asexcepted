/** Fixed feed row height (badge column + vertical breathing room). */
export const FEED_ROW_HEIGHT_PX = 96;
export const FEED_ROW_HEIGHT_CLASS = "h-24";

/** Badge art size inside the row (smaller than row — inset padding around it). */
export const FEED_BADGE_PX = 68;
